robot: drone

variables:
  for/global: for

functions:
  # ConvertToDegrees/global:
  #   input: radians **block
  #   body:
  #     include:
  #       namespace: X
  #       path: /var/



model:
  $('for'):
    iterator: a
    range: 1
    body:
      print: $(a)
  part: 
      name: base
      joint:
        type: fixed
        parent: root_link
        xyz:
        rpy:


      link:
        geometry: Box
        scale: 1 1 1
        xyz:
        rpy:
        collision


